---
# Kubeconfig path for east site
server_kubeconfig: "{{ ansible_env.HOME }}/.kube/config"
kubeconfig: "{{ server_kubeconfig }}"

# Namespace configuration
namespace_name: "{{ namespace_prefix }}-iperf3-hub"