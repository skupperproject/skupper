---
# Kubeconfig path for west site
kubeconfig_2: "{{ ansible_env.HOME }}/.kube/config"
kubeconfig: "{{ kubeconfig_2 }}"

# Namespace configuration
namespace_name: "{{ namespace_prefix }}-iperf3-client"

# Iperf3 client pod configuration
iper3_bandwidth: 1k
